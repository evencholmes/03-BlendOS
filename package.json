{
  "name": "blendos",
  "version": "5.0.0",
  "description": "BlendOS — Blender Skill Tracker. 100+ skills across 11 disciplines.",
  "scripts": {
    "build": "echo No build step needed — single file app",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "install-plugins": "npm install @capacitor/core@6 @capacitor/android@6 @capacitor/cli@6 && npm install @capacitor/filesystem@6 @capacitor/share@6 @capacitor/app@6 --legacy-peer-deps"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.2.1",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/share": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
